# Imagenia 项目上下文

## 当前目标

开发一款适配 QwenPaw 的图像插件：通过 OpenAI 图像 API 生成和编辑图片，并提供图片资料库、收藏和瀑布流浏览能力。

## 已确认决策

- 第一版功能：生成、编辑、收藏。
- 插件需要前端页面，也需要后端能力，因此不是纯 Frontend 插件，而是前后端一体的 `general` 插件候选。
- 后端负责 API Key、OpenAI 请求、图片元数据、收藏状态和资源访问；前端只负责交互和展示。
- 图片第一阶段只保存在 QwenPaw 本机；未来增加同步接口时，插件只负责上传，由服务器提供存储和同步能力。
- 图片和收藏采用全局共享，不按 Agent 或工作区隔离。
- 第一版以 OpenAI 图像 API 为唯一图像提供商，暂不抽象多提供商体系。

## 第一版建议决策

- **编辑范围**：只支持基于插件已生成图片的编辑；暂不支持任意本地图片上传、蒙版和多图输入。
- **删除能力**：允许删除图片，同时删除对应本地文件和元数据；暂不实现回收站和恢复。
- **生成模式**：采用后台任务队列，前端显示 `pending`、`running`、`succeeded`、`failed` 状态。先实现单任务处理，批量、重试和取消可后续增加。
- **API Key**：使用插件全局配置一份，由后端保存和读取；前端只显示脱敏状态，不回显完整密钥。
- **瀑布流内容**：默认展示全部图片，并提供“仅看收藏”筛选。只展示收藏会让资料库失去主要价值。
- **存储方式**：优先使用本地 SQLite 保存元数据和任务状态；图片二进制保存到插件专属本地目录。收藏直接作为图片资产状态保存。若 QwenPaw 提供稳定的持久化接口，再评估替换 SQLite。

## 核心领域词汇

- **ImageAsset（图片资产）**：插件管理的生成或编辑结果，是图片库中的核心对象。
- **GenerationJob（生成任务）**：一次异步生成或编辑请求及其执行状态；第一版一个任务最多产生一个图片资产。
- **SourceAsset（来源资产）**：编辑任务使用的已有图片资产；编辑结果通过 `source_asset_id` 与其建立来源关系。
- **Favorite（收藏状态）**：全局共享的图片筛选状态，不代表独立用户或收藏夹。
- **PluginDataDirectory（插件数据目录）**：SQLite 数据库和图片文件所在的 QwenPaw 本地专属目录。

## 已接受的核心架构

- 数据访问使用 SQLite、版本化迁移和 Repository 层，不引入 ORM。
- 任务使用 SQLite 持久化，由插件启动的单个后台 worker 串行执行。
- 第一版一个任务只产生一个图片资产。
- 前端轮询任务状态，暂不使用 SSE 或 WebSocket。
- 图片以 UUID 命名、按年月存放，并且只通过后端资源接口访问。
- 删除来源资产时不级联删除编辑结果，而是解除后代资产的来源引用。
- 前端采用单页面工作台，不拆分复杂路由。
- 详细理由见 `docs/adr/0001-core-plugin-architecture.md`。

## 第一版核心数据

- `image_assets`：图片、提示词、模型、尺寸、文件路径、来源图片和时间信息。
- `generation_jobs`：生成任务、状态、错误信息和结果图片 ID。
- `image_assets.is_favorite` / `favorited_at`：全局图片收藏状态。

## 后续扩展能力

以下能力不进入第一版，但保留在产品路线中：

- 提示词历史和重新生成；
- 图片版本对比与来源关系；
- 标签、搜索和筛选；
- 多个收藏夹；
- 图片详情页和更多元数据；
- 不同模型或服务商；
- 批量生成、任务重试、取消和并发控制；
- 本地图片同步上传接口；
- 远程服务器或对象存储适配。

## 设计状态

核心技术设计已收敛，待进入规格拆分和实现。已确认的隐私与工作台交互决策见 `docs/adr/0004-privacy-and-workbench-interactions.md`，完整技术设计见 `docs/technical-design.md`。


## 已接受的 API 与生命周期决策

- 第一版使用 OpenAI Images API，外部调用封装在适配器中。
- 收藏是 `ImageAsset` 状态，不建立独立收藏表。
- 启动时恢复 `pending`，将遗留 `running` 标记为 `failed/interrupted`，不自动重试。
- 删除图片时删除文件、解除后代来源引用，并保留脱敏的最小任务记录。
- API Key 优先读取 `IMAGENIA_OPENAI_API_KEY`，其次读取插件数据目录中权限为 `0600` 的私有配置文件；接口不返回完整密钥。
- 创建任务返回 `202 Accepted` 和 `job_id`，前端轮询 Job 资源。
- SQLite 使用事务化的前向版本迁移；迁移失败时阻止后端注册。
- 详细理由见 `docs/adr/0002-jobs-api-and-data-lifecycle.md`。


## 已接受的模型与界面边界

- 第一版固定后端配置的默认模型，前端不允许自由输入模型名称。
- 生成表单只开放 `prompt`、`size`（square/landscape/portrait）和 `quality`（standard/high）。
- 错误分为稳定错误码、用户安全文案和仅写日志的详细信息。
- 单 worker 串行执行，单任务总超时 10 分钟，最多 50 个 `pending` 任务，超出返回 `429`。
- 资产列表按 `created_at DESC, id DESC` 游标分页，默认每页 30 条。
- 图片详情显示未删除资产的提示词；编辑结果显示自己的提示词并关联来源资产。
- 编辑从已有资产详情进入，并创建新的资产，不覆盖原图。
- 详细理由见 `docs/adr/0003-model-and-ui-boundaries.md`。
